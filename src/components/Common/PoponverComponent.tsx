import React from 'react';
import {Popover} from "@mui/material";

const PoponverComponent = ({children}: { children: JSX.Element }) => {
    return (
        <div></div>
    );
};

export default PoponverComponent;